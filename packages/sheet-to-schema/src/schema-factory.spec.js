import { SchemaFactory } from './schema-factory.js'

describe('SchemaFactory', () => {
  test('creates a schema from workbook data using a schema adapter', () => {
    const factory = new SchemaFactory()
    // Write your test case here
  })
})
